<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <title>{{title}}</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/styles.css">

</head>
<body>
  <nav class="navbar navbar-expand-lg navbar-dark bg-success px-4 d-flex justify-content-between align-items-center">
    <a class="navbar-brand fw-bold text-white" href="/">Agrotienda</a>

    <form class="d-flex" role="search" action="/productos/buscar" method="get">
      <input class="form-control me-2" type="search" placeholder="Buscar productos" name="q">
      <button class="btn btn-outline-light" type="submit">Buscar</button>
    </form>

    <div class="d-flex align-items-center">
      {{#if session.usuario}}
        <a href="/carrito" class="btn btn-outline-light ms-3" title="Ver carrito">🛒</a>
        <span class="ms-3 fw-bold text-white">{{session.usuario}}</span>

        {{#if (eq session.rol 'admin')}}
          <a href="/productos/nuevo" class="btn btn-sm btn-outline-warning ms-2">Nuevo Producto</a>
        {{/if}}

        <a href="/usuarios/logout" class="btn btn-sm btn-outline-danger ms-2">Cerrar sesión</a>
      {{else}}
        <a href="/usuarios/login" class="btn btn-sm btn-outline-light ms-2">Iniciar sesión</a>
        <a href="/usuarios/registro" class="btn btn-sm btn-light ms-2">Registrarse</a>
      {{/if}}
    </div>
  </nav>

  <main class="container mt-4">
    {{{body}}}
  </main>
</body>
</html>
